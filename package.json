{
  "name": "pipelinekit",
  "private": true,
  "version": "0.1.1",
  "description": "A local AI production pipeline for Blender.",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @pipelinekit/desktop dev",
    "build": "pnpm -r build",
    "prebuild:desktop": "pnpm --filter @pipelinekit/core --filter @pipelinekit/recipes --filter @pipelinekit/assets -r build && pnpm --filter @pipelinekit/sidecar run bundle && pnpm --filter @pipelinekit/desktop exec vite build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "sync:version": "node tools/sync-version.mjs"
  },
  "packageManager": "pnpm@10.32.1",
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
